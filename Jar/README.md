To create a .jar file, open the application on your prefered IDE.
<br>
<br>
Open the terminal, then type "./mvnw package".
<br>
<br>
To run the .jar file, type on the terminal "java -jar JARFILE.jar".
